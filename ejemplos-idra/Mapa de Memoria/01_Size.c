/*	Ejemplo #1
	Para los ejemplos usaremos el comando size, que en su salida muestra el
    tamaño del segmento de texto, de datos inicializados, y el bss.
    
    size archivo.bin

	En este caso el segmento de datos contiene 544 bytes, mientras que el bss 
	solamente 8 bytes.
*/

#include <stdio.h>

int main() {

	return 0;

}
